<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>


<body>
    <%- include('./partials/nav.ejs') %>

    <div class="container">
        <div class="edit-card">
            <!-- SIDEBAR -->
            <div class="sidebar"></div>
            <!-- EDIT ITEM CONSTAINER -->
            <div class="edit-item">
                <h1 class="edit-head">NEW TASK</h1>
                <!-- FORM -->
                <form action="/items" method="POST">
                    <!-- TITLE -->
                    <fieldset class="edit-title">
                        <legend>TITLE</legend>
                        <input type="text" name="title" />
                    </fieldset>
                    <!-- DESCRIPTION -->
                    <fieldset class="edit-description">
                        <legend>DESCRIPTION</legend>
                        <textarea type="text" name="description" ></textarea>
                    </fieldset>
                    <!-- URGENT -->
                    <div class="edit-u-d">
                        <fieldset class="edit-urgent">
                            <legend>URGENT</legend>
                            <input class="checkbox" type="checkbox" name="urgent" />
                        </fieldset>
                        <!-- DUE -->
                        <fieldset class="edit-due">
                            <legend>DUE</legend>
                            <input type="text" name="dueDate" />
                        </fieldset>
                    </div>
                    <!-- UPDATE BUTTON -->
                    <input class="edit-update-btn" type="submit" value="ADD" />
                </form>
            </div>
        </div>
    </div>

    <%- include('./partials/footer.ejs') %>
</body>

</html>